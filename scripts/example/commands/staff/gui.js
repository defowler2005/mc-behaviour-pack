import { world } from '@minecraft/server';
import { commandBuilder } from '../../../library/build/classes/commandBuilder.js';

/**
 * Array of staff modules.
 * @type {Array<Object>}
 */
export const staffModules = [
    {
        displayName: '',
        moduleId: '',
        indexId: 0
    }
];